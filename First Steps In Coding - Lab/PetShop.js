function solve(input) {
    let dogFoodPrice = Number(input[0]) * 2.50;
    let catFoodPrice = Number(input[1]) * 4;
     
    let totalZooCost = (dogFoodPrice) + (catFoodPrice);
     
    console.log (`${totalZooCost} lv.`);
    }
